# Fine-Tuning Dependencies for Kafe Nusantara
# Install: pip install -r scripts/fine-tune/requirements.txt
#
# Note: Requires NVIDIA GPU with CUDA for training
# CPU-only training is possible but extremely slow

# Core fine-tuning
unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git
transformers>=4.45.0
datasets>=3.0.0
peft>=0.13.0
trl>=0.12.0

# Training utilities
accelerate>=1.0.0
bitsandbytes>=0.44.0
sentencepiece>=0.2.0

# PyTorch (CUDA 12.1) — adjust for your CUDA version
# For CPU only: torch>=2.4.0
torch>=2.4.0
